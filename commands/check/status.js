/***
  TODO This check should check
    - if a proxy is running, which app it is running with
    - if frontend components are enabled for the app, check if (sym/npm)links exist and are correct
      - if a npm start process is running for the frontend components
    - if other apps are running and proxied, check if the webpack processes are running
      - verify if dev servers are accessible over http/https and their given port
    ...
***/
