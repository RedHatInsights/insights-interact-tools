// TODO this should run a given app with (its) LOCAL_CHROME config (more investigation should be done)
// It might also be tricky to run the command as a child process.
// maybe we can exit this process and let npm run start:proxy take over?
